name: face_attendance
description: Ứng dụng điểm danh sinh viên bằng khuôn mặt
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  provider: ^6.1.1
  flutter_secure_storage: ^9.2.2
  qr_code_scanner: ^1.0.1
  camera: ^0.11.0
  image_picker: ^1.0.7
  path_provider: ^2.1.3
  google_mlkit_face_detection: ^0.10.0
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
